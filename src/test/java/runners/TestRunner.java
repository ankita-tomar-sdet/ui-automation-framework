package runners; // Ensure this matches your actual package folder structure

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class TestRunner {
    // This class intentionally left blank - JUnit Platform runs Cucumber via the engine
}